{
  "name": "cursor-slash-commands",
  "version": "1.0.0",
  "description": "Slash commands for tier workflow (features, phases, sessions, tasks)",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --max-warnings 0"
  },
  "keywords": ["cursor", "slash-commands", "tier-workflow", "appointment-workflow"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "glob": "^13.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "typescript-eslint": "^8.15.0"
  }
}
